# Dependencies and build output (do not commit)
node_modules/
dist/
*.local

# Environment and secrets (never commit)
.env
.env.*
.env.local
.env.development.local
.env.test.local
.env.production.local
config.local.json
*.pem
*.key
*.p12
*.pfx
id_rsa
id_rsa.pub

# IDE and editor (local only)
.cursor/
.vscode/
*.code-workspace
*.suo
*.user
*.userosscache

# Logs and caches (may contain paths or tokens)
*.log
npm-debug.log*
.npm
.cache/
.parcel-cache/
.vite/

# OS files
.DS_Store
Thumbs.db

# Test and coverage
coverage/
.nyc_output/
*.lcov
tokenize-path-output.wav

# Optional: large or generated assets (uncomment if you generate these locally)
# public/models/
# public/audio/static/

# Packed audio for Docker (upload to release/S3; do not commit)
audio-assets.tar.gz
